version: '3'

services:
    api:
        container_name: m5_panda_zap_API
        build:
            context: .
            dockerfile: api.Dockerfile
        ports:
            - "11005:11005"
        command: "go run main.go"
