version: '3'

services:

    server:
        container_name: a3_sstype
        build:
            context: ../
            dockerfile: deployments/sstype.Dockerfile
        ports:
            - "10001:10001"
        networks:
            - a3net
        restart: always

networks:
    a3net: